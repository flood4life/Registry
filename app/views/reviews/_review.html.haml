.col-sm-6
  .panel.panel-primary
    .panel-heading
      .row
        .col-xs-6.equal-height#vertical_hack
          %span.panel-title
            %span.text-muted
              by
              - if review.user.nil?
                %em guest
            = review.user.nil? ? review.guest_name : review.user.email
        .col-xs-6.equal-height
          - if can? :manage, review
            %span.pull-right
              = link_to 'Edit', edit_catalog_product_review_path(review.product.catalog, review.product, review), class: 'btn btn-warning'
              = link_to 'Delete', catalog_product_review_path(review.product.catalog, review.product, review), method: :delete, class: 'btn btn-danger'
    .panel-body
      = review.message