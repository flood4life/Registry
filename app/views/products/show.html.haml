%p#notice
  = notice

= link_to 'Edit', edit_product_path(@product)
= link_to 'Back', products_path

= render 'information'

= render partial: 'reviews/index', locals: {product: @product}

= render partial: 'reviews/form', locals: {product: @product, review: @product.reviews.new}

= link_to 'New Review', new_product_review_path(@product)