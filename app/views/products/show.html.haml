= link_to 'Edit', edit_product_path(@product)
= link_to 'Back', catalog_path(@product.catalog)

= render @product

= render partial: 'reviews'

- if can? :create, Review
  = render partial: 'reviews/form'