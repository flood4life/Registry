%h3 Reviews
%table.table.table-condensed
  - @product.reviews.approved.each do |review|
    %tr
      %td= review.message
      %td
        - if review.user
          = review.user.email
        - elsif review.guest_name.present?
          = review.guest_name
        - else
          %strong UFO
      - if can? :update, review
        %td= link_to 'Edit', edit_product_review_path(@product, review)
        %td= link_to 'Delete', product_review_path(@product, review), method: :delete